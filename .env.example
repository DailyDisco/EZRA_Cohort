# =================================================================
# EZRA APARTMENT MANAGEMENT SYSTEM - ENVIRONMENT VARIABLES
# =================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# =================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =================================================================
# PostgreSQL connection details
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppassword
POSTGRES_DB=appdb

# Database URL for backend (constructed automatically in docker-compose.yml)
PG_URL=postgresql://${POSTGRES_USER:-appuser}:${POSTGRES_PASSWORD:-apppassword}@postgres:${POSTGRES_PORT:-5432}/${POSTGRES_DB:-appdb}?sslmode=disable

# =================================================================
# BACKEND CONFIGURATION (Go)
# =================================================================
# Server port
PORT=8080

# Domain URL for utilities and API endpoints
DOMAIN_URL=http://localhost:8080

# Temporary directories for file processing
TEMP_DIR=./temp

# =================================================================
# AUTHENTICATION (Clerk)
# =================================================================
# Clerk authentication keys - Get these from https://clerk.com
CLERK_SECRET_KEY=your_clerk_secret_key_here
CLERK_WEBHOOK=your_clerk_webhook_secret_here

# Admin user details for initial setup
ADMIN_FIRST_NAME=Admin
ADMIN_LAST_NAME=User

# =================================================================
# FRONTEND CONFIGURATION (React/Vite)
# =================================================================
# API URL for frontend to communicate with backend
VITE_API_URL=http://localhost:8080

# Clerk publishable key for frontend - Get from https://clerk.com
VITE_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key_here

# Frontend port (for docker-compose)
FRONTEND_PORT=5173

# =================================================================
# EMAIL/SMTP CONFIGURATION
# =================================================================
# SMTP server settings (Amazon SES recommended)
SMTP_ENDPOINT_ADDRESS=email-smtp.us-east-1.amazonaws.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASSWORD=your_smtp_password
SMTP_TLS_MODE=starttls
SMTP_FROM=noreply@yourdomain.com
SMTP_TEST_EMAIL=test@yourdomain.com

# =================================================================
# DOCUMENT SIGNING (Documenso)
# =================================================================
# Documenso API configuration for lease document signing
DOCUMENSO_API_URL=https://your-documenso-instance.com/api/v1
DOCUMENSO_API_KEY=your_documenso_api_key
DOCUMENSO_PUBLIC_URL=https://your-documenso-instance.com
DOCUMENSO_WEBHOOK_SECRET=your_documenso_webhook_secret

# =================================================================
# AI/CHATBOT (OpenAI)
# =================================================================
# OpenAI API key for chatbot functionality
OPENAI_API_KEY=your_openai_api_key_here

# =================================================================
# CRON JOBS & AUTOMATION
# =================================================================
# Email address for cron job notifications
CRON_EMAIL=admin@yourdomain.com

# =================================================================
# DEVELOPMENT & DEBUGGING
# =================================================================
# Ngrok authentication token (for webhook testing)
NGROK_AUTHTOKEN=your_ngrok_auth_token_here

# =================================================================
# REQUIRED ENVIRONMENT VARIABLES CHECKLIST
# =================================================================
# ✅ DATABASE: POSTGRES_HOST, POSTGRES_PORT, POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, PG_URL
# ✅ BACKEND: PORT, DOMAIN_URL, TEMP_DIR
# ✅ AUTH: CLERK_SECRET_KEY, CLERK_WEBHOOK, ADMIN_FIRST_NAME, ADMIN_LAST_NAME
# ✅ FRONTEND: VITE_API_URL, VITE_CLERK_PUBLISHABLE_KEY, FRONTEND_PORT
# ✅ EMAIL: SMTP_ENDPOINT_ADDRESS, SMTP_PORT, SMTP_USER, SMTP_PASSWORD, SMTP_TLS_MODE, SMTP_FROM, SMTP_TEST_EMAIL
# ✅ DOCUMENTS: DOCUMENSO_API_URL, DOCUMENSO_API_KEY, DOCUMENSO_PUBLIC_URL, DOCUMENSO_WEBHOOK_SECRET
# ✅ AI: OPENAI_API_KEY
# ✅ CRON: CRON_EMAIL
# ✅ DEVELOPMENT: NGROK_AUTHTOKEN (optional for webhook testing)

# =================================================================
# SETUP INSTRUCTIONS
# =================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all required values marked with "your_*_here"
# 3. For production, ensure all secrets are properly secured
# 4. Test your configuration with: docker-compose up
